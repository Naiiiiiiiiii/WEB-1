# KIẾN TRÚC VÀ LUỒNG DỮ LIỆU DỰ ÁN SHOESTORE

## 📐 TỔNG QUAN KIẾN TRÚC

### 1. Kiến trúc 3 tầng (3-Tier Architecture)

```
┌─────────────────────────────────────────────────────────────┐
│                    PRESENTATION LAYER                        │
│                     (Tầng giao diện)                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  index.html  │  │admin-index   │  │product-detail│      │
│  │   + CSS      │  │   html+CSS   │  │  .html+CSS   │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                            ↕ (DOM Events / Rendering)
┌─────────────────────────────────────────────────────────────┐
│                    BUSINESS LOGIC LAYER                      │
│                     (Tầng xử lý logic)                      │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ProductManager │ UserManager │ OrderManager          │   │
│  │ cart.js        │ category.js │ inventory.js          │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                            ↕ (CRUD Operations)
┌─────────────────────────────────────────────────────────────┐
│                    DATA LAYER                                │
│                  (Tầng lưu trữ dữ liệu)                     │
│  ┌───────────────────────────────────────────────────┐     │
│  │            LocalStorage (Browser API)              │     │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐     │     │
│  │  │Products│ │ Users  │ │ Orders │ │  Cart  │     │     │
│  │  └────────┘ └────────┘ └────────┘ └────────┘     │     │
│  └───────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. SƠ ĐỒ QUAN HỆ GIỮA CÁC MODULE

```
                    ┌──────────────────┐
                    │    main.js       │
                    │  (Entry Point)   │
                    └────────┬─────────┘
                             │
                             ↓
         ┌───────────────────┴───────────────────┐
         │                                       │
         ↓                                       ↓
┌─────────────────┐                    ┌─────────────────┐
│ ProductManager  │←──────────────────→│   UserManager   │
│  ┌──────────┐  │                    │  ┌──────────┐  │
│  │ Product  │  │                    │  │   User   │  │
│  └──────────┘  │                    │  └──────────┘  │
└────────┬────────┘                    └────────┬────────┘
         │                                      │
         │                                      │
         ↓                                      ↓
┌─────────────────┐                    ┌─────────────────┐
│   cart.js       │←──────────────────→│  order-manager  │
│ (Giỏ hàng)     │                    │  (Đơn hàng)     │
└────────┬────────┘                    └────────┬────────┘
         │                                      │
         └──────────────┬───────────────────────┘
                        ↓
                ┌─────────────────┐
                │  LocalStorage   │
                └─────────────────┘
```

---

## 3. LUỒNG DỮ LIỆU CHI TIẾT

### 3.1. Luồng Khởi tạo Ứng dụng

```
1. Browser tải index.html
         ↓
2. Tải các file CSS (style.css, modal.css, ...)
         ↓
3. Tải các file JS với type="module"
   - main.js
   - ProductManager.js
   - cart.js
   - user.js
   - ...
         ↓
4. ProductManager khởi tạo
   ├→ Đọc LocalStorage: 'shoestore_products'
   ├→ Nếu có: Parse JSON → tạo Product instances
   └→ Nếu không: Load từ productData.js (dữ liệu mẫu)
         ↓
5. UserManager khởi tạo
   ├→ Đọc LocalStorage: 'users_shoestore'
   ├→ Nếu có: Parse JSON → tạo User instances
   └→ Nếu không: Tạo admin và testuser mặc định
         ↓
6. Kiểm tra session user
   ├→ Đọc LocalStorage: 'nguoiDungHienTai'
   ├→ Nếu có: Hiển thị "Xin chào [tên]"
   └→ Nếu không: Hiển thị icon đăng nhập
         ↓
7. Render giao diện
   ├→ Render danh sách sản phẩm (renderProducts.js)
   ├→ Cập nhật cart count (nếu có)
   └→ Khởi tạo event listeners
         ↓
8. Ứng dụng sẵn sàng
```

---

### 3.2. Luồng Thêm Sản phẩm vào Giỏ (Chi tiết)

```
USER CLICK "THÊM VÀO GIỎ"
         ↓
┌────────────────────────────────────────────────────────┐
│ 1. Lấy thông tin sản phẩm                              │
│    - productId từ data-attribute hoặc URL              │
│    - size từ dropdown (nếu có)                         │
│    - quantity từ input                                 │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│ 2. Gọi addToCart(productId, name, price, ...)         │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│ 3. Kiểm tra đăng nhập                                  │
│    getCurrentUsername() → đọc LocalStorage             │
│    'nguoiDungHienTai'                                  │
│    ├→ Nếu null: openLoginModal() → RETURN             │
│    └→ Nếu có: Tiếp tục                                │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│ 4. Lấy thông tin sản phẩm từ ProductManager            │
│    productManager.getProductById(productId)            │
│    ├→ Kiểm tra product có tồn tại không               │
│    └→ Kiểm tra product có variants không               │
└────────────────────────────────────────────────────────┘
         ↓
         ├─────────────┬─────────────┐
         │             │             │
         ↓             ↓             ↓
┌────────────┐ ┌─────────────┐ ┌────────────┐
│ Có variants│ │Có variants  │ │Không có    │
│ + Đã chọn │ │+ Chưa chọn  │ │variants    │
│   size    │ │   size      │ │            │
└─────┬──────┘ └──────┬──────┘ └─────┬──────┘
      ↓               ↓               ↓
┌─────────────┐ ┌─────────────┐ ┌─────────────┐
│Lấy variant  │ │Chỉ cho 1    │ │Lấy initial- │
│theo size    │ │dòng "Chưa   │ │Stock        │
│Kiểm tra     │ │chọn" qty=1  │ │Kiểm tra đủ  │
│stock >= qty │ │             │ │hàng         │
└─────┬───────┘ └──────┬──────┘ └─────┬───────┘
      └────────────────┴────────────────┘
                       ↓
┌────────────────────────────────────────────────────────┐
│ 5. Lấy giỏ hàng hiện tại                               │
│    getCart() → đọc LocalStorage 'cart_[username]'      │
│    Parse JSON → Array of cart items                    │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│ 6. Kiểm tra item đã có trong giỏ chưa                  │
│    const itemIdentifier = `${productId}-${size}`       │
│    const existing = cart.find(i =>                     │
│      i.itemIdentifier === itemIdentifier)              │
│    ├→ Nếu có: Tăng quantity                           │
│    └→ Nếu không: Push item mới vào cart               │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│ 7. Lưu giỏ hàng                                        │
│    localStorage.setItem(                               │
│      `cart_${username}`,                               │
│      JSON.stringify(cart)                              │
│    )                                                   │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│ 8. Cập nhật UI                                         │
│    - updateCartCount(): Cập nhật số trên icon giỏ     │
│    - Animation bounce cho icon                         │
│    - Hiển thị toast/alert "Đã thêm vào giỏ"          │
└────────────────────────────────────────────────────────┘
         ↓
       DONE
```

---

### 3.3. Luồng Đặt hàng (Checkout)

```
USER CLICK "TIẾN HÀNH THANH TOÁN"
         ↓
┌────────────────────────────────────────────────────────┐
│ 1. Kiểm tra giỏ hàng hợp lệ                            │
│    checkCartBeforeCheckout()                           │
│    ├→ Kiểm tra tất cả item đã chọn size chưa          │
│    ├→ Kiểm tra giỏ không rỗng                         │
│    └→ Nếu không hợp lệ: alert → RETURN                │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│ 2. Mở modal checkout                                   │
│    - Inject HTML modal vào body                        │
│    - Hiển thị danh sách sản phẩm trong giỏ            │
│    - Hiển thị tổng tiền                                │
│    - Focus vào ô nhập tên                              │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│ 3. User nhập thông tin                                 │
│    - Họ tên (required)                                 │
│    - Số điện thoại (required, pattern validation)      │
│    - Email (required, email validation)                │
│    - Địa chỉ (required, min 10 chars)                  │
│    - Phương thức thanh toán (select)                   │
└────────────────────────────────────────────────────────┘
         ↓
USER CLICK "XÁC NHẬN ĐẶT HÀNG"
         ↓
┌────────────────────────────────────────────────────────┐
│ 4. Validate form                                       │
│    - Kiểm tra tất cả field required đã điền            │
│    - Validate format (email, phone)                    │
│    - Nếu không hợp lệ: Hiển thị lỗi → RETURN          │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│ 5. Gọi placeOrder(orderData)                           │
│    orderData = {                                       │
│      customerInfo: { name, phone, email, address },    │
│      paymentMethod: 'COD' | 'Banking',                 │
│      items: getCart(),                                 │
│      total: calculateCartTotal()                       │
│    }                                                   │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│ 6. Kiểm tra tồn kho lần cuối (Critical Section)       │
│    FOR EACH item in cart:                              │
│      product = productManager.getProductById(item.id)  │
│      IF product có variants:                           │
│        variant = product.getVariant(item.size)         │
│        IF variant.stock < item.quantity:               │
│          → alert "Size X không đủ hàng"               │
│          → RETURN null (Hủy đơn)                       │
│      ELSE:                                             │
│        IF product.initialStock < item.quantity:        │
│          → alert "Sản phẩm không đủ hàng"            │
│          → RETURN null (Hủy đơn)                       │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│ 7. Trừ tồn kho (Transaction-like)                      │
│    FOR EACH item in cart:                              │
│      success = productManager.decreaseStock(           │
│        item.id,                                        │
│        item.quantity,                                  │
│        item.size                                       │
│      )                                                 │
│      - Cập nhật variants[i].stock HOẶC initialStock   │
│      - Lưu vào sales array (lịch sử)                  │
│      - Save vào LocalStorage                           │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│ 8. Tạo order object                                    │
│    order = {                                           │
│      id: `ORD-${Date.now()}`,                         │
│      username: getCurrentUsername(),                   │
│      customerInfo: { ... },                            │
│      items: cart,                                      │
│      total: orderData.total,                           │
│      date: new Date().toISOString(),                   │
│      status: 'new', // Đang chờ xử lý                 │
│      paymentMethod: orderData.paymentMethod            │
│    }                                                   │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│ 9. Lưu order vào LocalStorage                          │
│    orders = JSON.parse(                                │
│      localStorage.getItem('shoestore_orders') || '[]'  │
│    )                                                   │
│    orders.push(order)                                  │
│    localStorage.setItem(                               │
│      'shoestore_orders',                               │
│      JSON.stringify(orders)                            │
│    )                                                   │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│ 10. Lưu vào lịch sử user                               │
│     user = userManager.users.find(...)                 │
│     user.orders.push(order)                            │
│     userManager.luuDanhSachUser()                      │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│ 11. Xóa giỏ hàng                                       │
│     clearCart()                                        │
│     localStorage.removeItem(`cart_${username}`)        │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│ 12. Cập nhật UI                                        │
│     - Đóng modal checkout                              │
│     - Hiển thị modal thành công với order ID           │
│     - Reset cart count = 0                             │
│     - Nếu có callback: Gọi onOrderSuccess()           │
└────────────────────────────────────────────────────────┘
         ↓
       DONE - ORDER CREATED
```

---

### 3.4. Luồng Quản lý Tồn kho (Admin)

```
ADMIN NHẬP HÀNG
         ↓
┌────────────────────────────────────────────────────────┐
│ 1. Admin chọn sản phẩm từ dropdown                     │
│    <select id="importProductSelect">                   │
│      <option value="1">Giày Nike Air Max</option>     │
│      <option value="2">Giày Adidas Ultra Boost</option>│
│    </select>                                           │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│ 2. Kiểm tra sản phẩm có variants không                 │
│    product = productManager.getProductById(selectedId) │
│    IF product.variants.length > 0:                     │
│      → Hiển thị ô nhập size                           │
│    ELSE:                                               │
│      → Ẩn ô nhập size                                 │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│ 3. Admin nhập thông tin                                │
│    - Số lượng (required, min=1)                        │
│    - Giá nhập / Giá vốn (required, min=1000)          │
│    - Size (nếu có variants, required)                  │
│    - Ghi chú (optional)                                │
└────────────────────────────────────────────────────────┘
         ↓
ADMIN CLICK "HOÀN THÀNH PHIẾU NHẬP"
         ↓
┌────────────────────────────────────────────────────────┐
│ 4. Validate dữ liệu                                    │
│    - Quantity > 0                                      │
│    - ImportPrice > 0                                   │
│    - Size hợp lệ (nếu có variants)                    │
│    Nếu không hợp lệ: alert → RETURN                   │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│ 5. Gọi processProductImport()                          │
│    productManager.processProductImport(                │
│      productId,                                        │
│      quantity,                                         │
│      importPrice,                                      │
│      size, // null nếu không có variants              │
│      note                                              │
│    )                                                   │
└────────────────────────────────────────────────────────┘
         ↓
         ├─────────────┬─────────────┐
         │             │             │
         ↓             ↓             ↓
┌──────────────┐ ┌────────────┐ ┌──────────────┐
│Có variants   │ │Variants    │ │Không có      │
│+ Size tồn tại│ │chưa có size│ │variants      │
└──────┬───────┘ └──────┬─────┘ └──────┬───────┘
       ↓                ↓                ↓
┌──────────────┐ ┌────────────┐ ┌──────────────┐
│Tìm variant   │ │Thêm variant│ │Cập nhật      │
│theo size     │ │mới:        │ │initialStock  │
│Tăng stock:   │ │variants.   │ │initialStock  │
│variant.stock│ │push({size, │ │+= quantity   │
│+= quantity   │ │stock: qty})│ │              │
└──────┬───────┘ └──────┬─────┘ └──────┬───────┘
       └────────────────┴────────────────┘
                        ↓
┌────────────────────────────────────────────────────────┐
│ 6. Cập nhật giá vốn chung                              │
│    product.costPrice = importPrice                     │
│    (Giá vốn chung của sản phẩm)                        │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│ 7. Lưu lịch sử nhập                                    │
│    product.imports.push({                              │
│      date: new Date().toISOString(),                   │
│      qty: quantity,                                    │
│      costPrice: importPrice,                           │
│      note: note || "Nhập hàng thủ công",              │
│      type: "IMPORT",                                   │
│      variantSize: size                                 │
│    })                                                  │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│ 8. Lưu vào LocalStorage                                │
│    productManager.saveProducts()                       │
│    localStorage.setItem(                               │
│      'shoestore_products',                             │
│      JSON.stringify(products.map(p => p.toJSON()))     │
│    )                                                   │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│ 9. Cập nhật UI                                         │
│    - Render lại bảng tồn kho                           │
│    - Hiển thị thông báo thành công                     │
│    - Reset form nhập hàng                              │
│    - Nếu stock từ 0 → >0: Bỏ class 'out-of-stock'    │
└────────────────────────────────────────────────────────┘
         ↓
       DONE - STOCK UPDATED
```

---

## 4. CẤU TRÚC DỮ LIỆU CHI TIẾT

### 4.1. Product (Sản phẩm)

```javascript
{
  // === Thông tin cơ bản ===
  id: 1,                              // ID sản phẩm (Number)
  name: "Giày Nike Air Max 2024",    // Tên sản phẩm (String)
  categoryId: 1,                      // ID danh mục (Number)
  description: "Giày thể thao...",    // Mô tả (String)
  
  // === Hình ảnh ===
  img: "url_to_image.jpg",            // Hình chính (String URL)
  images: [                           // Mảng hình phụ (Array<String>)
    "url1.jpg",
    "url2.jpg",
    "url3.jpg"
  ],
  
  // === Giá cả ===
  price: 2500000,                     // Giá bán (Number, VNĐ)
  oldPrice: 3000000,                  // Giá cũ (Number, VNĐ, nullable)
  costPrice: 1500000,                 // Giá vốn (Number, VNĐ)
  targetProfitMargin: 25,             // % Lợi nhuận mong muốn (Number)
  
  // === Tồn kho ===
  initialStock: 100,                  // Tồn kho chung (Number)
                                     // CHỈ dùng khi KHÔNG có variants
  
  variants: [                         // Biến thể (Array<Object>)
    {
      size: 39,                       // Size giày (Number)
      stock: 10                       // Tồn kho của size này (Number)
    },
    {
      size: 40,
      stock: 15
    },
    {
      size: 41,
      stock: 0                        // Hết hàng
    }
  ],
  
  lowStockThreshold: 5,               // Ngưỡng cảnh báo tồn thấp (Number)
  
  // === Lịch sử giao dịch ===
  imports: [                          // Lịch sử nhập hàng (Array<Object>)
    {
      date: "2025-01-15T10:30:00.000Z", // ISO String
      qty: 50,                          // Số lượng nhập
      costPrice: 1500000,               // Giá nhập
      note: "Nhập từ nhà cung cấp A",  // Ghi chú
      type: "IMPORT",                   // Loại giao dịch
      variantSize: 40                   // Size (nullable)
    }
  ],
  
  sales: [                            // Lịch sử bán hàng (Array<Object>)
    {
      date: "2025-01-16T14:20:00.000Z",
      qty: 2,
      type: "SALE",
      variantSize: 40
    }
  ],
  
  // === Hiển thị ===
  rating: 4.5,                        // Đánh giá trung bình (Number 0-5)
  ratingCount: 128,                   // Số lượt đánh giá (Number)
  badge: "sale",                      // Nhãn (String: 'new', 'sale', 'hot')
  isHidden: false                     // Ẩn/Hiện (Boolean)
}
```

---

### 4.2. User (Người dùng)

```javascript
{
  hoTen: "Nguyễn Văn A",              // Họ tên (String)
  tenDangNhap: "nguyenvana",          // Username (String, unique)
  email: "nguyenvana@email.com",      // Email (String, unique)
  matKhau: "123456",                  // Password (String, plain text - chỉ demo!)
  
  orders: [                           // Lịch sử đơn hàng (Array<Order>)
    {
      id: "ORD-1705305000000",
      date: "2025-01-15T10:30:00.000Z",
      status: "delivered",
      total: 5000000,
      items: [/* ... */]
    }
  ],
  
  isLocked: false                     // Trạng thái khóa (Boolean)
}
```

---

### 4.3. Cart Item (Sản phẩm trong giỏ)

```javascript
{
  id: 1,                              // ID sản phẩm (Number)
  name: "Giày Nike Air Max",          // Tên sản phẩm (String)
  price: 2500000,                     // Giá (Number)
  img: "url_to_image.jpg",            // Hình ảnh (String URL)
  size: "40",                         // Size đã chọn (String)
                                     // Có thể là: "39", "40", "N/A", "Chưa chọn"
  color: "Đen",                       // Màu (String, hiện tại chưa dùng)
  quantity: 2,                        // Số lượng (Number)
  itemIdentifier: "1-40"              // ID duy nhất (String: "productId-size")
                                     // Dùng để phân biệt các item trong giỏ
}
```

**Lưu ý về itemIdentifier**:
- `"1-40"`: Sản phẩm ID 1, size 40
- `"1-41"`: Sản phẩm ID 1, size 41 (Khác item trên!)
- `"2-N/A"`: Sản phẩm ID 2, không có size
- `"3-Chưa chọn"`: Sản phẩm ID 3, chưa chọn size (cần chọn trước khi checkout)

---

### 4.4. Order (Đơn hàng)

```javascript
{
  id: "ORD-1705305000000",            // Mã đơn hàng (String)
                                     // Format: ORD-{timestamp}
  
  username: "nguyenvana",             // Username người đặt (String)
  
  customerInfo: {                     // Thông tin khách hàng (Object)
    name: "Nguyễn Văn A",
    phone: "0901234567",
    email: "nguyenvana@email.com",
    address: "123 Nguyễn Huệ, Q1, TP.HCM"
  },
  
  items: [                            // Danh sách sản phẩm (Array<CartItem>)
    {
      id: 1,
      name: "Giày Nike Air Max",
      price: 2500000,
      quantity: 2,
      size: "40",
      img: "url.jpg"
    },
    {
      id: 2,
      name: "Giày Adidas Ultra Boost",
      price: 2800000,
      quantity: 1,
      size: "41",
      img: "url2.jpg"
    }
  ],
  
  total: 7800000,                     // Tổng tiền (Number)
  
  date: "2025-01-15T10:30:00.000Z",   // Ngày đặt (String ISO)
  
  status: "new",                      // Trạng thái (String)
                                     // Các giá trị:
                                     // - "new": Đang chờ xử lý
                                     // - "processed": Đã xử lý
                                     // - "delivered": Đã giao
                                     // - "canceled": Đã hủy
  
  paymentMethod: "COD"                // Phương thức thanh toán (String)
                                     // - "COD": Thanh toán khi nhận hàng
                                     // - "Banking": Chuyển khoản
}
```

---

## 5. PHÂN TÍCH TÍNH NĂNG NỔI BẬT

### 5.1. Quản lý Biến thể (Variants)

**Vấn đề**: Cùng một mẫu giày nhưng có nhiều size, mỗi size có tồn kho riêng.

**Giải pháp**: Sử dụng mảng `variants` trong Product

**Ví dụ thực tế**:
```javascript
const product = {
  id: 1,
  name: "Giày Nike Air Max",
  variants: [
    { size: 39, stock: 5 },   // Size 39 còn 5 đôi
    { size: 40, stock: 10 },  // Size 40 còn 10 đôi
    { size: 41, stock: 0 }    // Size 41 hết hàng
  ]
};

// User muốn mua size 40
product.getVariant(40); // { size: 40, stock: 10 }

// User muốn mua size 41
product.getVariant(41); // { size: 41, stock: 0 } → Hiển thị "Hết hàng"

// Tính tổng tồn kho
product.getCurrentStock(); // 5 + 10 + 0 = 15
```

**Xử lý trong giỏ hàng**:
- Item A: productId=1, size=40 → itemIdentifier="1-40"
- Item B: productId=1, size=41 → itemIdentifier="1-41"
- Item A và B là 2 item khác nhau trong giỏ!

---

### 5.2. Quản lý Giá bán dựa trên % Lợi nhuận

**Vấn đề**: Admin muốn định giá bán tự động dựa trên giá vốn và % lợi nhuận mong muốn.

**Công thức**:
```
Giá Bán = Giá Vốn / (1 - (% Lợi nhuận / 100))
```

**Ví dụ**:
```javascript
// Giá vốn: 1.000.000đ
// Lợi nhuận mong muốn: 25%

Giá Bán = 1.000.000 / (1 - 0.25)
        = 1.000.000 / 0.75
        = 1.333.333đ
        ≈ 1.333.000đ (làm tròn đến 1000đ)

// Kiểm tra:
Lợi nhuận thực tế = (1.333.000 - 1.000.000) / 1.000.000 * 100
                  = 33.3% (Cao hơn 25% một chút do làm tròn)
```

**Code**:
```javascript
// price-admin.js
function calculateSalePrice(costPrice, marginPercent) {
  const newPrice = costPrice / (1 - (marginPercent / 100));
  const rounded = Math.round(newPrice / 1000) * 1000;
  return rounded;
}

// Ví dụ
calculateSalePrice(1000000, 25); // 1333000
calculateSalePrice(1500000, 30); // 2143000
calculateSalePrice(2000000, 20); // 2500000
```

---

### 5.3. Kiểm tra Tồn kho trước Checkout

**Vấn đề**: User thêm sản phẩm vào giỏ lúc còn hàng, nhưng đến lúc checkout thì hết hàng rồi.

**Giải pháp**: Kiểm tra tồn kho 2 lần
1. **Lần 1**: Khi thêm vào giỏ (ở client)
2. **Lần 2**: Khi đặt hàng (critical, quyết định có tạo order không)

**Code kiểm tra lần 2**:
```javascript
// order-manager.js
function placeOrder(orderData) {
  const cart = getCart();
  
  // Kiểm tra TỪNG item trong giỏ
  for (const item of cart) {
    const product = productManager.getProductById(item.id);
    
    // Lấy tồn kho hiện tại
    let currentStock;
    if (product.variants.length > 0) {
      const variant = product.getVariant(item.size);
      currentStock = variant ? variant.stock : 0;
    } else {
      currentStock = product.initialStock;
    }
    
    // So sánh với số lượng muốn mua
    if (currentStock < item.quantity) {
      alert(`"${item.name}" ${item.size !== 'N/A' ? `size ${item.size}` : ''} chỉ còn ${currentStock} sản phẩm!`);
      return null; // Hủy đơn hàng
    }
  }
  
  // Nếu tất cả đều OK, tiếp tục xử lý đơn hàng
  // ...
}
```

---

## 6. BEST PRACTICES ÁP DỤNG

### 6.1. Tách biệt Concerns

```javascript
// ❌ BAD: Trộn lẫn logic và UI
function addProduct() {
  const name = document.getElementById('name').value;
  const price = document.getElementById('price').value;
  
  products.push({ name, price });
  localStorage.setItem('products', JSON.stringify(products));
  
  alert('Đã thêm!');
  document.getElementById('name').value = '';
}

// ✅ GOOD: Tách biệt
// Model + Controller
class ProductManager {
  addProduct(data) {
    const product = new Product(data);
    this.products.push(product);
    this.saveProducts();
    return product;
  }
}

// View
function handleAddProductFormSubmit(e) {
  e.preventDefault();
  const formData = getFormData(e.target);
  
  const product = productManager.addProduct(formData);
  
  if (product) {
    showSuccessMessage('Đã thêm sản phẩm!');
    resetForm(e.target);
    renderProducts();
  }
}
```

---

### 6.2. Sử dụng Constants

```javascript
// ✅ GOOD: Định nghĩa constants
const STORAGE_KEYS = {
  PRODUCTS: 'shoestore_products',
  USERS: 'users_shoestore',
  ORDERS: 'shoestore_orders',
  CURRENT_USER: 'nguoiDungHienTai',
  ADMIN_USER: 'nguoiDungAdmin'
};

const ORDER_STATUS = {
  NEW: 'new',
  PROCESSED: 'processed',
  DELIVERED: 'delivered',
  CANCELED: 'canceled'
};

// Sử dụng
localStorage.setItem(STORAGE_KEYS.PRODUCTS, data);
order.status = ORDER_STATUS.NEW;
```

---

### 6.3. Error Handling

```javascript
// ✅ GOOD: Luôn xử lý lỗi
function loadProducts() {
  try {
    const data = localStorage.getItem(STORAGE_KEYS.PRODUCTS);
    
    if (!data) {
      console.warn('Không tìm thấy dữ liệu sản phẩm, load dữ liệu mẫu');
      return loadSampleProducts();
    }
    
    const products = JSON.parse(data);
    
    if (!Array.isArray(products)) {
      throw new Error('Dữ liệu sản phẩm không hợp lệ');
    }
    
    return products.map(p => Product.fromJSON(p));
    
  } catch (error) {
    console.error('Lỗi khi tải sản phẩm:', error);
    alert('Có lỗi khi tải dữ liệu. Vui lòng thử lại.');
    return [];
  }
}
```

---

## 7. CÁCH TRÌNH BÀY KIẾN TRÚC VỚI GIẢNG VIÊN

### Script trình bày mẫu:

**"Dạ, em xin trình bày về kiến trúc của dự án ạ:**

**1. Tổng quan:**
- Dự án áp dụng mô hình MVC 3 tầng
- Tầng 1: Presentation (HTML/CSS)
- Tầng 2: Business Logic (JavaScript Classes)
- Tầng 3: Data (LocalStorage)

**2. Module hóa:**
- Em chia code thành 29 file JavaScript
- Mỗi file có một trách nhiệm cụ thể
- Sử dụng ES6 Modules để import/export

**3. Flow chính:**
- User thao tác trên UI → Event handlers
- Handlers gọi Controllers (ProductManager, cart.js)
- Controllers xử lý logic và CRUD data
- Data được lưu vào LocalStorage
- UI được cập nhật để phản ánh thay đổi

**4. Điểm nổi bật:**
- Quản lý biến thể sản phẩm (variants)
- Tính toán giá bán tự động
- Kiểm tra tồn kho 2 lần (khi thêm giỏ và checkout)
- Phân biệt session user và admin

**5. Ưu điểm:**
- Code rõ ràng, dễ bảo trì
- Không cần backend để demo
- Áp dụng OOP và design patterns

**Dạ, đó là tổng quan về kiến trúc của em ạ. Em xin phép được demo luồng hoạt động cụ thể nếu thầy/cô muốn ạ."**

---

## KẾT LUẬN

Tài liệu này cung cấp cái nhìn chi tiết về:
- Kiến trúc tổng thể của hệ thống
- Luồng dữ liệu giữa các component
- Cấu trúc dữ liệu chi tiết
- Best practices được áp dụng

Hy vọng giúp bạn hiểu sâu hơn về cách các phần của dự án hoạt động cùng nhau! 🎯
